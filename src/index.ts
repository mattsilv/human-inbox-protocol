// HIP reference implementation — library entry point.
// The CLI lives in src/cli/index.ts (the `hip` binary). This module re-exports
// the pieces a programmatic embedder might want; it stays intentionally thin.

export const HIP_VERSION = "0.1.0";
export const HIP_SCHEMA_VERSION = "0.1";
